[workspace]
members = [
  "utils/core",
  "utils/rand",
  "math",
  "crypto",
  "fri",
  "air",
  "prover",
  "verifier",
  "winterfell"
]


[profile.release]
codegen-units = 1
lto = true

[profile.bench]
codegen-units = 1
lto = true
